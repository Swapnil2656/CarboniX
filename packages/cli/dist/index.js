#!/usr/bin/env node
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// src/index.ts
var import_commander = require("commander");
var import_picocolors = __toESM(require("picocolors"));
var import_axios = __toESM(require("axios"));
var import_promises = __toESM(require("fs/promises"));
var import_path = __toESM(require("path"));
var program = new import_commander.Command();
var CONFIG_PATH = import_path.default.join(process.cwd(), ".carbonixrc");
var API_URL = process.env.CARBONIX_API_URL || "http://localhost:4000/api/v1";
program.name("carbonix").description("CarboniX Command Line Interface").version("0.1.0");
var CONFIG_JS_CONTENT = (apiKey, baseUrl) => `/**
 * CarboniX Configuration
 * Generated by: npx @carbonix/cli init
 * \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
 * Set CARBONIX_API_KEY in your .env to override the key below.
 * Get your key from your CarboniX Dashboard.
 */
const Carbonix = require('carbonix').default;

const cx = new Carbonix({
  apiKey:  process.env.CARBONIX_API_KEY  || '${apiKey}',
  baseUrl: process.env.CARBONIX_BASE_URL || '${baseUrl}',
  timeoutMs: 10_000,
});

module.exports = { cx };
`;
var TELEMETRY_JS_CONTENT = `/**
 * CarboniX Telemetry Reporter
 * Generated by: npx @carbonix/cli init
 * \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
 * Reports live CPU metrics to your CarboniX Dashboard every 5 minutes.
 * The AI Analyst flags idle/oversized instances and suggests savings.
 */
const os = require('os');
const { cx } = require('./carbonix.config');

function getCpuUtilization() {
  return new Promise(resolve => {
    const t1 = os.cpus();
    setTimeout(() => {
      const t2 = os.cpus();
      let idle = 0, total = 0;
      t1.forEach((cpu, i) => {
        for (const type in t2[i].times) total += t2[i].times[type] - cpu.times[type];
        idle += t2[i].times.idle - cpu.times.idle;
      });
      resolve(parseFloat((1 - idle / total).toFixed(4)));
    }, 100);
  });
}

function startTelemetry({ instanceId, instanceType, provider, region, projectName, intervalMs = 5 * 60 * 1000, storageGb = 0 }) {
  const report = async () => {
    try {
      const cpuUtilization = await getCpuUtilization();
      const record = await cx.ingest({ instanceId, instanceType, provider, region, cpuUtilization, storageGb, projectName });
      console.log(\`[CarboniX] \u{1F4CA} \${record.carbonKg.toFixed(4)} kg CO\u2082 | CPU: \${(cpuUtilization * 100).toFixed(1)}% | idle: \${record.isIdle}\`);
    } catch (err) {
      console.warn('[CarboniX] Telemetry failed (non-fatal):', err.message);
    }
  };
  report();
  const timer = setInterval(report, intervalMs);
  if (timer.unref) timer.unref();
  console.log(\`[CarboniX] \u2705 Reporting every \${intervalMs / 1000}s for \${instanceId}\`);
  return { stop: () => clearInterval(timer) };
}

module.exports = { startTelemetry };
`;
var ENTRY_FILE_CANDIDATES = [
  "server.js",
  "server.ts",
  "index.js",
  "index.ts",
  "app.js",
  "app.ts",
  "src/server.js",
  "src/server.ts",
  "src/index.js",
  "src/index.ts",
  "src/app.js",
  "src/app.ts",
  "main.js",
  "main.ts"
];
async function detectEntryFile(cwd) {
  try {
    const pkg = JSON.parse(await import_promises.default.readFile(import_path.default.join(cwd, "package.json"), "utf-8"));
    if (pkg.main && await fileExists(import_path.default.join(cwd, pkg.main))) return pkg.main;
  } catch (_) {
  }
  for (const c of ENTRY_FILE_CANDIDATES) {
    if (await fileExists(import_path.default.join(cwd, c))) return c;
  }
  return null;
}
async function fileExists(p) {
  try {
    await import_promises.default.access(p);
    return true;
  } catch {
    return false;
  }
}
async function detectEnvVars(cwd) {
  const vars = {};
  try {
    const lines = (await import_promises.default.readFile(import_path.default.join(cwd, ".env"), "utf-8")).split("\n");
    for (const line of lines) {
      const m = line.match(/^([^#=]+)=(.*)$/);
      if (m) vars[m[1].trim()] = m[2].trim().replace(/^["']|["']$/g, "");
    }
  } catch (_) {
  }
  return vars;
}
function buildInjectionSnippet(cfg) {
  return [
    "",
    "// \u2500\u2500\u2500 CarboniX Auto-injected by @carbonix/cli (do not remove) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
    "const { startTelemetry } = require('./carbonix-telemetry');",
    "startTelemetry({",
    `  instanceId:   process.env.INSTANCE_ID    || '${cfg.instanceId}',`,
    `  instanceType: process.env.INSTANCE_TYPE  || '${cfg.instanceType}',`,
    `  provider:     process.env.CLOUD_PROVIDER || '${cfg.provider}',`,
    `  region:       process.env.AWS_REGION     || '${cfg.region}',`,
    `  projectName:  process.env.APP_NAME       || '${cfg.projectName}',`,
    "});",
    "// \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
    ""
  ].join("\n");
}
program.command("init").description("Initialize CarboniX in your project \u2014 scaffolds config, telemetry reporter, and auto-injects tracking").requiredOption("-k, --key <key>", "Your CarboniX API key (required)").option("--base-url <url>", "Override the CarboniX API URL", "http://localhost:4000").action(async (options) => {
  const cwd = process.cwd();
  const apiKey = options.key;
  const baseUrl = options.baseUrl;
  console.log("");
  console.log(import_picocolors.default.cyan("\u{1F33F} CarboniX \u2014 initializing your project..."));
  console.log("");
  process.stdout.write(import_picocolors.default.gray("  Verifying API key... "));
  try {
    const res = await import_axios.default.post(`${baseUrl}/api/v1/carbon/verify-key`, {}, {
      headers: { "Authorization": `Bearer ${apiKey}` }
    });
    if (!res.data.success) throw new Error("Key invalid");
    console.log(import_picocolors.default.green("\u2705 Valid"));
  } catch (err) {
    console.log(import_picocolors.default.red("\u274C Failed"));
    console.error(import_picocolors.default.red(`
  Error: ${err.response?.data?.error || err.message}`));
    console.error(import_picocolors.default.gray("  Make sure your API key is correct and your CarboniX server is running."));
    process.exit(1);
  }
  const envVars = await detectEnvVars(cwd);
  let provider = "aws";
  if (process.env.GOOGLE_CLOUD_PROJECT || envVars.GOOGLE_CLOUD_PROJECT) provider = "gcp";
  if (process.env.ARM_REGION || envVars.AZURE_REGION) provider = "azure";
  if (process.env.VERCEL || envVars.VERCEL) provider = "vercel";
  if (process.env.NETLIFY || envVars.NETLIFY) provider = "netlify";
  if (process.env.RAILWAY_ENVIRONMENT || envVars.RAILWAY_ENVIRONMENT) provider = "railway";
  if (process.env.RENDER || envVars.RENDER) provider = "render";
  const region = envVars.AWS_REGION || envVars.GCP_REGION || envVars.AZURE_REGION || process.env.AWS_REGION || "ap-south-1";
  const instanceId = envVars.INSTANCE_ID || require("os").hostname();
  const instanceType = envVars.INSTANCE_TYPE || "t3.medium";
  const projectName = envVars.APP_NAME || (() => {
    try {
      return require(import_path.default.join(cwd, "package.json")).name;
    } catch {
      return import_path.default.basename(cwd);
    }
  })();
  const cfg = { instanceId, instanceType, provider, region, projectName };
  const rcPath = import_path.default.join(cwd, ".carbonixrc");
  await import_promises.default.writeFile(rcPath, JSON.stringify({ apiKey, provider, region, baseUrl }, null, 2));
  console.log(import_picocolors.default.green("  \u2705  Created  .carbonixrc"));
  const configPath = import_path.default.join(cwd, "carbonix.config.js");
  if (!await fileExists(configPath)) {
    await import_promises.default.writeFile(configPath, CONFIG_JS_CONTENT(apiKey, baseUrl));
    console.log(import_picocolors.default.green("  \u2705  Created  carbonix.config.js"));
  } else {
    console.log(import_picocolors.default.gray("  \u23ED\uFE0F   Skipped  carbonix.config.js (already exists)"));
  }
  const telemetryPath = import_path.default.join(cwd, "carbonix-telemetry.js");
  if (!await fileExists(telemetryPath)) {
    await import_promises.default.writeFile(telemetryPath, TELEMETRY_JS_CONTENT);
    console.log(import_picocolors.default.green("  \u2705  Created  carbonix-telemetry.js"));
  } else {
    console.log(import_picocolors.default.gray("  \u23ED\uFE0F   Skipped  carbonix-telemetry.js (already exists)"));
  }
  const entryFile = await detectEntryFile(cwd);
  const MARKER = "CarboniX Auto-injected";
  if (entryFile) {
    const entryPath = import_path.default.join(cwd, entryFile);
    const entryContent = await import_promises.default.readFile(entryPath, "utf-8");
    if (entryContent.includes(MARKER)) {
      console.log(import_picocolors.default.gray(`  \u23ED\uFE0F   Skipped  auto-inject (already in ${entryFile})`));
    } else {
      await import_promises.default.appendFile(entryPath, buildInjectionSnippet(cfg));
      console.log(import_picocolors.default.green(`  \u2705  Injected  startTelemetry() \u2192 ${entryFile}`));
      console.log(import_picocolors.default.gray(`              provider: ${provider} | region: ${region}`));
      console.log(import_picocolors.default.gray(`              instanceType: ${instanceType} | project: ${projectName}`));
    }
  } else {
    console.log(import_picocolors.default.yellow("  \u26A0\uFE0F   Entry file not auto-detected. Add this to your server manually:"));
    console.log(import_picocolors.default.gray("       const { startTelemetry } = require('./carbonix-telemetry');"));
    console.log(import_picocolors.default.gray("       startTelemetry({ instanceId: 'my-server', instanceType: 't3.medium', provider: 'aws', region: 'ap-south-1' });"));
  }
  console.log("");
  console.log(import_picocolors.default.green("\u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2557"));
  console.log(import_picocolors.default.green("\u2551       \u{1F33F}  CarboniX initialized successfully!             \u2551"));
  console.log(import_picocolors.default.green("\u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2563"));
  console.log(import_picocolors.default.green("\u2551                                                          \u2551"));
  console.log(import_picocolors.default.green(`\u2551  API Key:  ${apiKey.substring(0, 20)}...${" ".repeat(Math.max(0, 27 - apiKey.substring(0, 20).length))}\u2551`));
  console.log(import_picocolors.default.green(`\u2551  Provider: ${provider.padEnd(10)} Region: ${region.padEnd(16)}\u2551`));
  console.log(import_picocolors.default.green("\u2551                                                          \u2551"));
  console.log(import_picocolors.default.green("\u2551  \u2705  .carbonixrc                                        \u2551"));
  console.log(import_picocolors.default.green("\u2551  \u2705  carbonix.config.js                                 \u2551"));
  console.log(import_picocolors.default.green("\u2551  \u2705  carbonix-telemetry.js                              \u2551"));
  console.log(import_picocolors.default.green("\u2551  \u2705  startTelemetry() injected into your entry file     \u2551"));
  console.log(import_picocolors.default.green("\u2551                                                          \u2551"));
  console.log(import_picocolors.default.green("\u2551  \u{1F680} Start your server \u2014 live data appears on Dashboard! \u2551"));
  console.log(import_picocolors.default.green("\u2551  Run: carbonix app analyze   \u2014 to see AI suggestions    \u2551"));
  console.log(import_picocolors.default.green("\u2551  Run: carbonix app deploy    \u2014 to trigger optimization  \u2551"));
  console.log(import_picocolors.default.green("\u2551                                                          \u2551"));
  console.log(import_picocolors.default.green("\u255A\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255D"));
  console.log("");
});
var authCmd = program.command("auth").description("Authentication commands");
authCmd.command("login").description("Log into your CarboniX account").action(() => {
  console.log(import_picocolors.default.cyan("\nTo authenticate, please visit your dashboard and generate an API key:"));
  console.log(import_picocolors.default.white("http://localhost:3000/admin/dashboard\n"));
  console.log(import_picocolors.default.gray("Then run: carbonix init --key <your-api-key>\n"));
});
var appCmd = program.command("app").description("App management commands");
appCmd.command("analyze").description("Analyze local codebase and get Agentic AI region recommendations without deploying").option("-p, --project <project>", "Target project name").action(async (options) => {
  try {
    let config = {};
    try {
      const configData = await import_promises.default.readFile(CONFIG_PATH, "utf-8");
      config = JSON.parse(configData);
    } catch (e) {
      console.error(import_picocolors.default.red("Error: Project not initialized. Run `carbonix init --key <your-api-key>` first."));
      process.exit(1);
    }
    if (!config.apiKey) {
      console.error(import_picocolors.default.red("Error: API Key not found in .carbonixrc."));
      process.exit(1);
    }
    console.log(import_picocolors.default.cyan(`
\u{1F50D} CarboniX Agentic AI: Analyzing Local Environment...`));
    let currentRegion = config.region || "us-east-1";
    let currentProvider = config.provider || "aws";
    const baseTelemetry = {
      projectName: options.project || "local-analysis",
      instanceType: "t3.medium",
      provider: currentProvider,
      region: currentRegion,
      cpuUtilization: 0.2,
      // typical baseline
      storageGb: 20
    };
    try {
      const recRes = await import_axios.default.post(`${API_URL}/carbon/recommend`, baseTelemetry, {
        headers: {
          "Authorization": `Bearer ${config.apiKey}`,
          "Content-Type": "application/json"
        }
      });
      if (recRes.data.success) {
        if (recRes.data.data.recommended) {
          const rec = recRes.data.data.recommended;
          console.log(import_picocolors.default.yellow(`
\u26A0\uFE0F  Optimization Found!`));
          console.log(import_picocolors.default.white(`Current Target : ${currentProvider.toUpperCase()} (${currentRegion})`));
          console.log(import_picocolors.default.white(`Current Carbon : ${(rec.co2KgMonth + rec.savingsKg).toFixed(2)} kg CO2e / month`));
          console.log(import_picocolors.default.green(`Recommended    : ${currentProvider.toUpperCase()} (${rec.region})`));
          console.log(import_picocolors.default.green(`New Carbon     : ${rec.co2KgMonth.toFixed(2)} kg CO2e / month`));
          console.log(import_picocolors.default.cyan(`
\u2728 Potential Savings: ~${Math.round(rec.reductionPercent)}% carbon reduction (${rec.savingsKg.toFixed(2)} kg)`));
          console.log(import_picocolors.default.gray(`
Run \`carbonix app deploy\` and the Agentic AI will automatically apply this optimization!`));
        } else {
          console.log(import_picocolors.default.green(`
\u2713 Your environment is already fully optimized!`));
          console.log(import_picocolors.default.gray(`Current Target : ${currentProvider.toUpperCase()} (${currentRegion})`));
          console.log(import_picocolors.default.gray(`You are running in the greenest available region for your provider.`));
        }
      }
    } catch (e) {
      console.error(import_picocolors.default.red(`Error fetching analysis: ${e.message}`));
    }
  } catch (err) {
    console.error(import_picocolors.default.red(`Error: ${err.message}`));
  }
});
appCmd.command("deploy").description("Deploy the current codebase and send telemetry to CarboniX").option("-p, --project <project>", "Target project name").action(async (options) => {
  try {
    let config = {};
    try {
      const configData = await import_promises.default.readFile(CONFIG_PATH, "utf-8");
      config = JSON.parse(configData);
    } catch (e) {
      console.error(import_picocolors.default.red("Error: Project not initialized. Run `carbonix init --key <your-api-key>` first."));
      process.exit(1);
    }
    if (!config.apiKey) {
      console.error(import_picocolors.default.red("Error: API Key not found in .carbonixrc."));
      process.exit(1);
    }
    let projectName = options.project;
    if (!projectName) {
      try {
        const pkgData = await import_promises.default.readFile(import_path.default.join(process.cwd(), "package.json"), "utf-8");
        projectName = JSON.parse(pkgData).name;
      } catch (e) {
        projectName = "unknown-project";
      }
    }
    console.log(import_picocolors.default.cyan(`
\u{1F4E6} Packaging codebase for deployment...`));
    let currentRegion = config.region || "us-east-1";
    let currentProvider = config.provider || "aws";
    const baseTelemetry = {
      projectName,
      instanceType: "t3.medium",
      // typical node environment
      provider: currentProvider,
      region: currentRegion,
      cpuUtilization: Math.random() * 0.4 + 0.1,
      // simulated CPU usage 10-50%
      storageGb: 20
    };
    console.log(import_picocolors.default.cyan(`
\u{1F916} Agentic AI analyzing deployment plan...`));
    try {
      const recRes = await import_axios.default.post(`${API_URL}/carbon/recommend`, baseTelemetry, {
        headers: {
          "Authorization": `Bearer ${config.apiKey}`,
          "Content-Type": "application/json"
        }
      });
      if (recRes.data.success && recRes.data.data.recommended) {
        const rec = recRes.data.data.recommended;
        if (rec.reductionPercent > 10 && rec.region !== currentRegion) {
          console.log(import_picocolors.default.green(`
\u2728 Agentic AI Intercepting Deployment \u2728`));
          console.log(import_picocolors.default.green(`Switching region from ${currentRegion} to ${rec.region} (within ${currentProvider.toUpperCase()})`));
          console.log(import_picocolors.default.gray(`Expected savings: ~${Math.round(rec.reductionPercent)}% carbon reduction`));
          currentRegion = rec.region;
          baseTelemetry.region = currentRegion;
          if (currentProvider.toLowerCase() === "vercel") {
            try {
              const vercelConfigPath = import_path.default.join(process.cwd(), "vercel.json");
              const vercelData = await import_promises.default.readFile(vercelConfigPath, "utf-8");
              const vercelJson = JSON.parse(vercelData);
              if (vercelJson.regions && Array.isArray(vercelJson.regions)) {
                vercelJson.regions = [rec.region];
                await import_promises.default.writeFile(vercelConfigPath, JSON.stringify(vercelJson, null, 2));
                console.log(import_picocolors.default.yellow(`\u{1F4DD} Agentic AI automatically updated vercel.json to use region: ${rec.region}`));
              }
            } catch (err) {
              console.log(import_picocolors.default.gray(`(Note: No local vercel.json found to rewrite, skipping file update)`));
            }
          }
        } else {
          console.log(import_picocolors.default.gray(`\u2713 Deployment plan optimized. Staying in ${currentRegion}.`));
        }
      }
    } catch (e) {
      console.log(import_picocolors.default.gray(`Could not fetch AI recommendation, proceeding with default region...`));
    }
    console.log(import_picocolors.default.yellow(`
\u2601\uFE0F  Deploying and uploading telemetry to CarboniX edge (project: ${projectName}, region: ${currentRegion})...`));
    const res = await import_axios.default.post(`${API_URL}/carbon/telemetry/ingest`, baseTelemetry, {
      headers: {
        "Authorization": `Bearer ${config.apiKey}`,
        "Content-Type": "application/json"
      }
    });
    if (res.data.success) {
      console.log(import_picocolors.default.green("\u{1F680} Deployment successful!"));
      console.log(import_picocolors.default.gray(`Carbon calculation generated: ${res.data.data.carbonKg.toFixed(2)} kg CO2e`));
    } else {
      console.error(import_picocolors.default.red("Error: Deployment failed."));
    }
  } catch (err) {
    console.error(import_picocolors.default.red(`Error connecting to CarboniX: ${err.response?.data?.error || err.message}`));
  }
});
var teamCmd = program.command("team").description("Team management commands");
teamCmd.command("sync").description("Automatically discover and sync codebase contributors to CarboniX").action(async () => {
  try {
    let config = {};
    try {
      const configData = await import_promises.default.readFile(CONFIG_PATH, "utf-8");
      config = JSON.parse(configData);
    } catch (e) {
      console.error(import_picocolors.default.red("Error: Project not initialized. Run `carbonix init --key <your-api-key>` first."));
      process.exit(1);
    }
    if (!config.apiKey) {
      console.error(import_picocolors.default.red("Error: API Key not found in .carbonixrc."));
      process.exit(1);
    }
    let projectName = "unknown-project";
    try {
      const pkgData = await import_promises.default.readFile(import_path.default.join(process.cwd(), "package.json"), "utf-8");
      projectName = JSON.parse(pkgData).name;
    } catch (e) {
    }
    console.log(import_picocolors.default.cyan(`
\u{1F50D} Analyzing local git history for contributors...`));
    const { execSync } = require("child_process");
    let gitOutput = "";
    try {
      gitOutput = execSync('git log --format="%an|%ae" | sort -u', { encoding: "utf-8" });
    } catch (e) {
      console.log(import_picocolors.default.yellow(`Warning: Not a git repository or git not installed. Could not extract authors.`));
      process.exit(0);
    }
    const rawAuthors = gitOutput.split("\n").filter((l) => l.trim() !== "");
    const members = rawAuthors.map((line) => {
      const [name, email] = line.split("|");
      return { name, email };
    }).filter((m) => {
      const email = m.email?.toLowerCase() || "";
      return m.name && email && !email.includes("dependabot") && !email.includes("bot@");
    });
    if (members.length === 0) {
      console.log(import_picocolors.default.yellow(`No human contributors found.`));
      return;
    }
    console.log(import_picocolors.default.cyan(`Found ${members.length} contributors. Syncing to CarboniX...`));
    const res = await import_axios.default.post(`${API_URL}/admin/users/sync`, { members, projectName }, {
      headers: {
        "Authorization": `Bearer ${config.apiKey}`,
        "Content-Type": "application/json"
      }
    });
    if (res.data.success) {
      console.log(import_picocolors.default.green(`\u2705 Successfully synced ${res.data.count} team members!`));
      console.log(import_picocolors.default.gray(`Visit your CarboniX dashboard to view their emission ratings.`));
    } else {
      console.error(import_picocolors.default.red("Error: Sync failed."));
    }
  } catch (err) {
    console.error(import_picocolors.default.red(`Error connecting to CarboniX: ${err.response?.data?.error || err.message}`));
  }
});
program.parse(process.argv);
if (!process.argv.slice(2).length) {
  program.outputHelp();
}
