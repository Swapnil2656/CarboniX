[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "cd /app && npm install --workspaces --include-workspace-root"
]

[phases.build]
cmds = [
  "cd /app/services/api && npx tsc"
]

[start]
cmd = "node /app/services/api/dist/index.js"
